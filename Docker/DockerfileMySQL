# Usa l'immagine ufficiale di MySQL
FROM mysql:latest

# Imposta variabili d'ambiente per la configurazione del database
ENV MYSQL_ROOT_PASSWORD=rootpassword
ENV MYSQL_DATABASE=mydb
ENV MYSQL_USER=myuser
ENV MYSQL_PASSWORD=mypassword

# Espone la porta MySQL standard
EXPOSE 3306

# Copia script di inizializzazione (opzionale)
# COPY init.sql /docker-entrypoint-initdb.d/

# Il comando di avvio è gestito dall'immagine base MySQL
